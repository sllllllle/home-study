<div class='memo memo-lg'><span class='masking-tape'></span>
  <div class='post-box'>
    <p class="guide">
      好きなことを50字以内で発信しましょう！
    </p>
    <%= form_with(model: @micropost, local: true) do |f| %>
      <%= render 'layouts/error_messages', model: f.object %>
      <%= f.text_area :content %>
      <%= f.submit 'Tweet!', class: 'tweet-btn' %>
    <% end %>
  </div>
  <div class='feed'>
    <ul class='nav nav-tabs nav-justified'>
      <li class='nav-item'><%= link_to 'すべて', microposts_path %></li>
      <li class='nav-item'><%= link_to 'フォロー', following_posts_micropost_path(@current_user) %></li>
    </ul>
    <div class="microposts scroll">
      <%= render 'microposts/microposts', microposts: @microposts %>
    </div>
  </div>
</div>